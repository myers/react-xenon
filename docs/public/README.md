# Public directory for static assets

Files in this directory are copied as-is to the built site by VitePress.

Note: Examples are NOT built here. They are built separately and copied
directly to dist-pages/examples/ after VitePress builds, to avoid being
processed by VitePress's SPA router.
